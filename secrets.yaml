# Local development secrets, paired with config.toml. Optional file. These
# are local-only placeholder credentials for a Postgres instance running on
# localhost, not real secrets.

postgresql_username: wordgame
postgresql_password: wordgame
