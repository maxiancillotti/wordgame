# Secrets baked into the wordgame Docker image (see ../Dockerfile), paired
# with config.toml in this directory. These are local-only placeholder
# credentials matching docker-compose.yml's postgres service, not real
# secrets - never reuse this file or its values outside local development.

postgresql_username: wordgame
postgresql_password: wordgame
